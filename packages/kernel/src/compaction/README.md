# Transcript window compaction.

Piruse owns thresholds and the ViewState note. Pi still runs cut-point, summary, and drive.
Change reason: shrinking the session log to fit the model window. Not memory.
